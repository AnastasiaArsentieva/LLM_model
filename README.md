# LLM_model
Исследовательская работа по созданию чат-бота в Телеграмм. В работе приведён переход от малых рекуррентных моделей (SLM — Small Language Models) к полноценной LLM (Large Language Models) на основе RuGPT-3.


**В результате работы создана структура:**

1.	rugpt3 _50k – папка с итогами обучения мини-модели на небольшом датасете 50К
2.	rugpt3 _cpu.v1 – папка с итогами обучения мини-модели
3.	rugpt3_medium_100k_context – папка с обучением медиум-модели на датасете в 100К
4.	rugpt3_medium_CLEAN_model – папка с обучением медиум-модели с улучшенным датасетом на 100К
5.	parser_dataset – парсим датасет для обучения модели (русские диалоги)
6.	russian_dialogues.parquet – полученный датасет
7.	create_dataset – создаем датасет для обучения модели
8.	cleaner_text – чистим все данные для создания датасета
9.	TenzorFlow_chatbot – создание чатбота с помощью библиотеки TenzorFlow (обучение)
10.	PyTorch_chatbot – создание чатбота с помощью библиотеки PyTorch (обучение)
11.	model_learn – обучение модели
12.	train_trasformer – создание чатбота с помощью библиотеки trasformer (обучение) на базе мини-модели
13.	train_medium_context - создание чатбота с помощью библиотеки trasformer (обучение) на базе медиум-модели
14.	Perplexity – расчет метрики эффективности модели
15.	SYSTEM_PROMPT – тестирование обученной модели
16.	tg_bot – добавление модели к боту в телеграмм, для промышленного использования

**Цель:**

Создать ИИ помощника, который мог бы отвечать по теме вопроса, запоминать, о чем был разговор, собирать из текста логические цепочки для ответа

**Задачи:**

1.	Подготовить датасет для обучения
2.	Выбрать наиболее эффективную модель
3.	Оценить качество работы модели
4.	Скорректировать и финализировать 

**Выполнено:**

**1.	Подготовка датасета для обучения**

Для обучения был найден в свободном доступе датасет русских диалогов russian_dialogues.parquet.
Данный сет приведен к виду Вопрос – Ответ.
В ходе обучения были выявлены «грязные данные», «шум в данных», которые бот активно использовал в общении после обучения.
Поэтому датасет предварительно очищался от дат и времени, имен при пересылке сообщений, тегов и тп данных и подавался заново на обучение модели.

**2.	Выбор наиболее эффективной модели**

Для сравнения и выбора наиболее эффективной модели изначально в рамках эксперимента использовались библиотеки PyTorch и TenzorFlow. В результате оценки качества моделей было принято решение перейти к Transformers .

При использовании библиотек обучалась модель Seq2Seq(), это классическая архитектура Sequence-to-Sequence (Seq2Seq) на базе рекуррентных нейронных сетей (RNN). 

**Роль библиотеки PyTorch в реализации чат-бота (скрипт PyTorch_chatbot)**

Использование фреймворка PyTorch позволило автоматизировать полный цикл разработки модели: от подготовки сырых данных до инференса (генерации ответов в реальном времени).

**1. Подготовка данных (torch.utils.data)**

Для эффективной подачи данных в нейронную сеть использованы встроенные инструменты PyTorch:

**•	Dataset:** Создан пользовательский класс ChatDataset, который инкапсулирует логику токенизации и преобразования текста в тензоры (torch.tensor).

**•	DataLoader:** Позволяет разбивать датасет на мини-батчи (в данном случае по 64 примера), перемешивать данные (shuffle=True) и организовывать параллельную загрузку, что критично для обучения       на больших выборках (500 000 строк). 

**2. Построение вычислительного графа (torch.nn)**

Библиотека torch.nn предоставляет высокоуровневые блоки для сборки архитектуры:

**•	nn.Module:** Базовый класс, который отслеживает все обучаемые веса модели.

**•	nn.Embedding:** Слой для перевода индексов слов в векторное пространство (эмбеддинги).

**•	nn.LSTM:** Реализация рекуррентной логики, которая умеет сохранять контекст диалога через скрытые состояния (h,c)open paren h comma c close paren (ℎ,𝑐).

**•	nn.Linear:** Выходной слой для классификации токенов. 

**3. Оптимизация и обучение (torch.optim)**

Процесс обучения реализован через автоматическое дифференцирование:

**•	optim.Adam:** Адаптивный оптимизатор, который обновляет веса модели на основе вычисленных градиентов.

**•	loss.backward():** Автоматический расчет градиентов методом обратного распространения ошибки, что избавляет от необходимости вручную вычислять сложные производные для LSTM. 

**4. Динамическая генерация текста (Inference)**

Одной из ключевых особенностей реализации чат-бота является использование PyTorch для авторегрессионной генерации:

**•	torch.no_grad():** Отключает вычисление градиентов для экономии памяти и ускорения работы во время общения с пользователем.

**•	F.softmax и torch.multinomial:** Библиотека torch.nn.functional используется для преобразования сырых выходов (логитов) в вероятности. Применение «температуры» позволяет сделать ответы бота           более разнообразными и человекоподобными, избегая зацикливания. 

________________________________________
**Связь с концепцией чат-бота**

С точки зрения архитектуры чат-бота, PyTorch решает задачу обучения с учителем (Supervised Learning) на парах «вопрос-ответ». 
Компонент скрипта /	Функция в чат-боте

**Encoder**	«Понимает» смысл вопроса пользователя и сжимает его в контекстный вектор.

**Decoder**	Пошагово предсказывает наиболее вероятное следующее слово ответа.

**Temperature (0.7)** Регулирует «креативность» бота: чем ниже значение, тем более строгие и предсказуемые ответы.

**torch.save**	Позволяет сохранить «память» (веса) обученного бота в файл для последующего использования без переобучения.

________________________________________
**Практический вывод**

Использование PyTorch позволило реализовать гибкий конвейер обработки естественного языка (NLP). Благодаря тензорным вычислениям на CUDA (GPU), модель способна быстро обрабатывать большие массивы диалоговых данных, а модульная структура nn.Module упрощает масштабирование архитектуры (например, добавление слоев внимания или увеличение глубины LSTM).

**Роль библиотеки TensorFlow в реализации чат-бота (скрипт TenzorFlow_chatbot)**

В скрипте TensorFlow (и его высокоуровневый API Keras) выступает в роли декларативного фреймворка, где архитектура описывается как граф потоков данных.

**1. Обработка текста и векторизация (tf.keras.preprocessing)**
В отличие от PyTorch-скрипта, здесь активно используются встроенные инструменты Keras для работы с текстом:

**•	Tokenizer:** Автоматизирует создание частотного словаря, фильтрацию знаков препинания и приведение к нижнему регистру.

**•	pad_sequences:** Стандартизирует длину векторов, дополняя их нулями (post-padding), что необходимо для формирования стабильных входных тензоров. 
    
**2. Functional API для архитектуры Seq2Seq**

TensorFlow использует Functional API (Model(inputs, outputs)), что особенно удобно для Seq2Seq моделей:

**•	Разделение состояний:** Библиотека позволяет легко извлекать внутренние состояния LSTM (state_h, state_c) через параметр return_state=True.

**•	Многовходовые модели:** Keras прозрачно обрабатывает два входа — для энкодера (вопрос) и для декодера (ответ), объединяя их в единый вычислительный граф. 

**3. Инференс через разделение моделей**

В TensorFlow-скрипте реализован классический подход: после обучения одна общая модель разделяется на две независимые:

**•	Encoder Model:** Для получения вектора мысли из входной фразы.

**•	Decoder Model:** Которая в цикле while предсказывает по одному слову, принимая на вход тензоры состояний от предыдущего шага. 

________________________________________

**Сравнение PyTorch vs TensorFlow и переход к Transformers**

**1. PyTorch vs TensorFlow: В чем разница?**

характеристика / 	PyTorch /	TensorFlow

**Стиль написания	Императивный (Object-Oriented).** / Код выглядит как обычный Python. Легче отлаживать. /	Декларативный (Graph-based). Вы сначала строите «рельсы» (граф), а потом пускаете по ним данные.

**Гибкость** /	Максимальная. Вы сами описываете цикл обучения и шаг оптимизатора. /	Высокая абстракция. Метод .fit() скрывает детали, делая код чище, но менее гибким.

**Инференс** /	Часто требует ручного управления тензорами и состояниями в каждом цикле. /	Предлагает четкую структуру разделения моделей через Functional API.

**2. Почему мир отказался от RNN (LSTM) в пользу Transformers?**

Несмотря на то, что код на LSTM рабочий, современные чат-боты (как ChatGPT) используют архитектуру Transformer.

Основные причины:

**1.	Проблема «забывания» (Long-range dependencies):**

**o	LSTM:** Пытается сжать весь смысл длинного предложения в один вектор фиксированного размера (h,ch comma c ℎ,𝑐). К концу длинной фразы начало предложения начинает «стираться».

**o	Transformer:** Использует механизм Attention (Внимание), который позволяет каждому слову «смотреть» на любое другое слово в предложении напрямую, независимо от расстояния.

**2.	Последовательная vs Параллельная обработка:**

**o	LSTM:** Нельзя вычислить 10-е слово, не вычислив предыдущие 9 (это рекурсия). Это делает обучение медленным.

**o	Transformer:** Обрабатывает все слова в предложении одновременно (параллельно). Это позволило использовать мощь видеокарт (GPU) на 100% и обучать модели на гигантских объемах данных (весь интернет).

**3.	Исчезающий градиент:**

o	В очень длинных цепочках LSTM градиент при обратном распространении затухает, и модель перестает учиться. Трансформеры решают эту проблему с помощью остаточных связей (Residual Connections). 

**Итог исследования:**

Хотя архитектуры на базе LSTM в средах PyTorch и TensorFlow являются фундаментом для понимания Seq2Seq моделей, они ограничены в работе с длинными контекстами. Переход к Transformer стал революцией, так как механизм внимания (Attention) заменил рекурсию, позволив моделям обучаться быстрее и глубже понимать сложные связи в языке.

**Обучение на базе архитектуры Transformer (библиотека Hugging Face)**

В данном подходе вместо построения модели с нуля используется Pre-trained (предобученная) модель RuGPT-3. Процесс превращается из «обучения с нуля» в Fine-tuning (дообучение под конкретную задачу диалога). В нашем случае взят обычный диалог пользователей сети. Можно взять конкретные диалоги под специфику бизнеса и создать помощника-энциклопедиста.

**1. Использование предобученной модели (Causal Language Modeling)**

В скрипте используется класс AutoModelForCausalLM.

**•	Механизм:** В отличие от Seq2Seq (где есть отдельный энкодер и декодер), модели типа GPT являются Decoder-only. Они обучаются предсказывать следующее слово на основе всех предыдущих.
**•	Формат данных:** Вопрос и ответ объединяются в одну строку: Вопрос: {Q} Ответ: {A} <|endoftext|>. Модель учится понимать структуру диалога как единую последовательность.

**2. Токенизация и специальные токены**

Используется AutoTokenizer, соответствующий модели. Важная деталь:

**•	eos_token:** Токен конца последовательности (<|endoftext|>). Он критически важен для Трансформера, так как сообщает модели, где заканчивается осмысленный ответ, чтобы она не генерировала текст бесконечно.

**3. Экосистема Hugging Face (Datasets & Trainer)**

Скрипт задействует высокоуровневые инструменты, которые автоматизируют сложные вычисления:

**•	Библиотека datasets:** Используется режим streaming, позволяющий обрабатывать огромные наборы данных (100 000 строк) без загрузки их целиком в оперативную память.

**•	Класс Trainer:** Это «движок» обучения. Он берет на себя цикл по эпохам, вычисление функции потерь и обновление весов.

**•	DataCollatorForLanguageModeling:** Автоматически формирует батчи и создает "маски", чтобы модель предсказывала только следующие токены, а не те, что уже известны.

**4. Оптимизация под CPU (Gradient Accumulation)**

Так как обучение идет на процессоре (use_cpu=True), применены важные техники оптимизации:

**•	gradient_accumulation_steps=8:** Позволяет имитировать большой размер батча на слабом железе. Модель делает 8 маленьких шагов, накапливает ошибку и только потом обновляет веса. Это стабилизирует обучение.

**•	per_device_train_batch_size=1:** Минимизирует потребление оперативной памяти.

________________________________________

**Итоговое сравнение: Почему это лучше LSTM?**

**Критерий /	Seq2Seq (LSTM)	/ Transformer (RuGPT-3)**
**Знания** /	Знает только те 500к фраз, что ты дал. /	Уже знает русский язык, грамматику и факты о мире (из интернета).
**Контекст** /	Забывает начало длинного вопроса. /	Механизм Self-Attention видит все связи между словами одновременно.
**Сложность** /	Нужно самому писать циклы и слои. /	Достаточно дообучить готовую мощную модель под свой стиль.
**Качество** /	Ответы часто сухие и грамматически неверные. /	Генерирует связный, «человечный» и разнообразный текст.
________________________________________

**Общий вывод:**

Переход к использованию архитектуры Transformer (модель RuGPT-3) позволил применить методику Fine-tuning. Вместо обучения базовым правилам языка, модель фокусируется на специфике диалогового взаимодействия. Благодаря механизму внимания (Attention) и предобученным весам, итоговый чат-бот демонстрирует значительно более высокий уровень понимания контекста и естественности ответов по сравнению с рекуррентными сетями (LSTM).


**3.	Оценка качество работы модели**

Без расчета метрик – оценка качества работы проводилась простым диалогом с обученной моделью и анализом ответов, насколько они логичны, связанны, насколько модель запоминает, что ей говорили, насколько может поддержать разговор.

При обучении – уделялось внимание динамике метрик (loss и grand-norm), скачкам, выходу на плато, дабы избежать переобучение модели и зазубривания ею текстовых ответов.

При сравнении двух моделей LSTM производилась оценка метрикой BLEU (Bilingual Evaluation Understudy). 

<img width="721" height="220" alt="image" src="https://github.com/user-attachments/assets/294253a2-00af-41f9-99fc-e3dbfa80d970" />

Сравнительный анализ показал явный компромисс между качеством генерации и производительностью.

**1.	Производительность:** Модель на базе PyTorch показала лучшую скорость отклика (6.63 мс), что делает её более подходящей для высоконагруженных систем реального времени. Это обусловлено меньшими накладными расходами фреймворка на инференс.

**2.	Качество:** Модель на Keras достигла более высокого значения метрики BLEU (0.0101). Несмотря на то, что оба значения остаются низкими из-за отсутствия механизма внимания в архитектуре LSTM, преимущество Keras объясняется более эффективной предобработкой данных встроенными инструментами библиотеки.

**3.	Общий вывод:** Низкие значения BLEU для обеих рекуррентных моделей подтверждают необходимость перехода к архитектуре Transformer (такой как RuGPT-3), которая способна лучше улавливать семантические связи, сохраняя при этом приемлемую скорость генерации»

В то время как BLEU измеряет «схожесть слов», Perplexity измеряет «степень уверенности» модели в своих ответах. Так как со словами при общении с ботом проблем у модели RuGPT-3  не возникало, то для оценки обученной модели RuGPT-3 была использована метрика Perplexity (PPL). (скрипт Perplexity)

<img width="665" height="171" alt="image" src="https://github.com/user-attachments/assets/71d958c2-5d53-49f9-a732-c46740884813" />

В отличие от BLEU, ориентированной на жесткое совпадение токенов, PPL позволяет оценить внутреннюю согласованность модели и её способность предсказывать вероятностное распределение слов в контексте.
Полученное значение PPL = 30.18 при значении функции потерь (Loss) 3.407 свидетельствует о высокой степени сходимости модели. Данный результат указывает на то, что модель успешно адаптировалась к специфике русского разговорного языка и способна генерировать логически связные ответы с низкой степенью неопределенности. На данных показателях было решено остановить обучение по имеющемуся датасету.

**4.	Корректировка создания чат-бота**

Корректировка производилась на основании получаемых результатов. Таким образом был произведен переход на RuGPT-3 и произведена дополнительная чистка датасета.

Примеры общения с промежуточнообученной моделью, снятые метрики 

<img width="600" height="227" alt="image" src="https://github.com/user-attachments/assets/8c9829fa-c689-43f4-84f4-338e76399915" />

<img width="915" height="1069" alt="image" src="https://github.com/user-attachments/assets/1a379345-e3db-4844-bf44-ea8552cfbc7c" />

<img width="974" height="588" alt="image" src="https://github.com/user-attachments/assets/ef7012c2-b929-4f6d-9e3e-9447da2baa38" />

<img width="973" height="460" alt="image" src="https://github.com/user-attachments/assets/d2ab6239-88cd-4c19-8bc8-fd8bcaf81e94" />

<img width="974" height="395" alt="image" src="https://github.com/user-attachments/assets/9a24380b-8521-458c-8a9c-77878a54e780" />

<img width="974" height="559" alt="image" src="https://github.com/user-attachments/assets/978d2956-2121-48b1-9436-c76a596c2760" />

<img width="973" height="219" alt="image" src="https://github.com/user-attachments/assets/79379fd1-7533-45ee-980e-954686f0c193" />

<img width="975" height="318" alt="image" src="https://github.com/user-attachments/assets/7d3f08b7-2678-4e1d-aed6-381b69891407" />

<img width="658" height="165" alt="image" src="https://github.com/user-attachments/assets/27e319a3-6413-40eb-b7e8-01e9854bdcf3" />

 
 




